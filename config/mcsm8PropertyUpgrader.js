module.exports = function upgradeMCSM8Property(localProperties){
    const { http_port, 
        http_ip, 
        ftp_is_allow, 
        ftp_port, 
        ftp_ip, 
        ftp_start_port, 
        ftp_end_port, 
        console_send_times, 
        console_max_out, 
        is_gzip, 
        is_allow_csrf, 
        login_url, 
        terminalQueue_max_size, 
        data_center_times, 
        customize_commande, 
        session_max_age, 
        schedule_max, 
        max_eac_task_limit, 
        allow_status_api
    } = localProperties;
}
